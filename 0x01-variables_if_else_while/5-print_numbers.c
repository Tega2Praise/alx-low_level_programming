#include <stdio.h>

/**
 * main - entry point
 * print - all single numbers of base 10
 * Return: always 0
 */
int main(void)
{
	int count = 0;

	while (count < 9)
	{
	printf("count is: %d\n", count);
	count++;
	}
	return (0);
}
